task: in_hospital_mortality
seed: 42
quick: false

data:
  embeddings: false

imputation:
  method: gain
  iterations: 10000
  batch_size: 128
  hint_rate: 0.9
  alpha: 100

model:
  type: xgboost_imputed
  params:
    n_estimators: 2000
    max_depth: 6
    learning_rate: 0.05
    early_stopping_rounds: 50
    n_jobs: 4

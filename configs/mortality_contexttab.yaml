task: in_hospital_mortality
seed: 42
quick: false

data:
  embeddings: false

# missingness:
#   mechanism: MCAR   # MCAR | MAR | MNAR
#   rate: 0.3
#   strength: 1.0
#   columns: null
#   drivers_map: null

model:
  type: contexttab
  params:
    bagging: 8              # sap-rpt-oss bagging ensemble size ("auto" | int)
    max_context_rows: 8192  # wrapper-side subsample + library safeguard
